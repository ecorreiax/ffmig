migration M {
  change {
    add_foreign_key :posts, :users, column: :author_id, on_delete: :cascade
    add_foreign_key :posts, :posts, column: :parent_id, name: "posts_parent_fkey"
    remove_foreign_key :posts, :users, column: :editor_id, on_delete: :nullify
  }
}
