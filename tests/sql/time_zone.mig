migration M {
  change {
    create_table :events {
      datetime :at, time_zone: true, default: :now
      timestamps time_zone: true
    }
    change_column :events, :at, :datetime, from: :datetime, from_time_zone: true
  }
}
