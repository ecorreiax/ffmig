migration M {
  change {
    add_index :users, :email, unique: true
    add_index :users, :email, name: "users_email_key"
    remove_index :users, :name
    remove_index :users, :slug, unique: true, name: "users_slug_key"
  }
}
