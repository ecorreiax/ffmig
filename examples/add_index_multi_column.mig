# add_index on several columns, in index order: named
# `index_events_on_user_id_and_created_at` by default. `where:` makes it
# partial, covering only the rows where the SQL condition holds.
migration AddEventIndexes {
  change {
    add_index :events, [:user_id, :created_at]
    add_index :users, :email, unique: true, where: "deleted_at IS NULL"
  }
}
