# add_foreign_key: points a column that already exists at another table's
# `id`. It adds no column and no index.
migration AddPostsAuthorForeignKey {
  change {
    add_foreign_key :posts, :users, column: :author_id, on_delete: :cascade
  }
}
